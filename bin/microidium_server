package microidium_server;

# VERSION

use lib '../lib';
use lib 'lib';
use Microidium::Server;
use DB::Skip pkgs => [
    qw( IO::Async::Stream IO::Async::Handle IO::Async::Notifier IO::Async::Loop
      IO::Async::Loop::Select IO::Async::Internals::TimeQueue
      IO::Async::Timer::Periodic)
];

Microidium::Server->new->run;

1;
